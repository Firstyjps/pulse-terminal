node_modules/
.next/
dist/
.turbo/
*.log
.env
.env.local
.env*.local
.DS_Store
.vscode/
.idea/
coverage/
.tsbuildinfo
*.tsbuildinfo

# Reference projects — kept locally for cross-check, not part of the monorepo build
_legacy/
Bybit Api/
Option Scan/
option-dashboard/

# Claude Code worktrees (ephemeral)
.claude/worktrees/

# Bundled MCP archive (rebuild via `pnpm --filter @pulse/mcp pack-dxt`)
apps/mcp/pulse-terminal.dxt

# Alert worker run-time data
apps/alerts/data/

# Realtime hub run-time data — persisted regime snapshot + atomic-write tmp
apps/realtime/data/last-regime.json
apps/realtime/data/last-regime.json.tmp

# pm2 logs + state
logs/
.pm2/

# Prompt drafts (compose in editor, pipe to claude — local only)
_prompts/

# tmux orchestration prompt files (worker dispatch payloads — local only)
.coordinator/

# Tauri (desktop wrapper) — Rust target is huge, gen/ is auto-regenerated
src-tauri/target/
src-tauri/gen/
src-tauri/icons/
src-tauri/Cargo.lock
# stub frontendDist (required by Tauri config) — keep tracked
!src-tauri/dist/
!src-tauri/dist/**
